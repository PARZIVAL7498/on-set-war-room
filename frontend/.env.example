# Copied into Vercel project settings (or use Vercel dashboard env UI).
# VITE_* vars are baked in at build time.

# Render backend URL, no trailing slash — e.g. https://on-set-war-room-api.onrender.com
VITE_API_BASE=
